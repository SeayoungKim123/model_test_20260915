# 작업 방식

- 파일을 수정하는 작업은 **묻지 않고** 워크트리(EnterWorktree)에서 수행한다.
  - 메인 트리에서 작업하는 것은 대표님이 "메인에서 해" 같이 별도로 지시할 때만이다.
  - 질문 답변, 코드 읽기, 조사만 하는 세션에서는 워크트리를 만들지 않는다.
- 워크트리 이름은 feat/<주제> 브랜치명과 맞춘다. 주제는 영문(예: feat/friends-tab) — 한글은 이름에 쓸 수 없다.
- 워크트리에 들어가면 **작업을 시작하기 전에** 개발 환경(로컬 호스트)을 먼저 준비하고 접속 주소를 알린다. 그 다음 확인을 기다리지 않고 작업을 시작한다. 작업이 끝난 뒤에 서버를 띄우는 순서는 금지.
  - 메인 트리의 `.env`를 복사하고 `npm install`을 실행한다(git 미추적이라 워크트리에 없다).
  - `npm run dev`를 백그라운드로 띄우고 "워크트리 서버: localhost:XXXX"를 명시한다. 메인 서버(3000)와 동시에 띄워 비교할 수 있다.
- 작업이 끝나면 채택(master에 merge 후 워크트리 삭제)·폐기(워크트리만 삭제)는 대표님이 정한다. push는 대표님이 직접 한다.